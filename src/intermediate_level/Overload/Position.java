package intermediate_level.Overload;

// Enum para representar os cargos fixos da empresa.
// Usar um Enum evita erros como digitar "Gerente" em vez de "GERENTE".

public enum Position {
    ANALYST_JR,
    ANALYST_PL,
    ANALYST_SR,
    DEVELOPER_JR,
    DEVELOPER_PL,
    DEVELOPER_SR
}